# ServerRack

**Fabric • Minecraft 1.21.1 • Java 21**

Съёмочный мод для роликов: живая серверная комната — мультиблок-стойки с дымом,
офисные ПК-сетапы с аварийной видеостеной, АК-уголок с экраном загрузки GTA 6
и креслом, комнатный декор и режиссёрский детонатор с одиннадцатью видами
взрывов (включая жёлтый ядерный гриб), бэкапом сцены на диск и послойным
восстановлением с уборкой последствий.

Made by Denchy / ehalo.

---

## Оглавление

1. [Установка](#установка)
2. [Модуль 1: Серверные стойки](#модуль-1-серверные-стойки)
3. [Модуль 2: ПК-сетап и видеостена](#модуль-2-пк-сетап-и-видеостена)
4. [Модуль 3: Режиссёрский детонатор](#модуль-3-режиссёрский-детонатор)
5. [Модуль 4: АК-сетап — загрузка GTA 6](#модуль-4-ак-сетап--загрузка-gta-6)
6. [Модуль 5: Комнатный декор](#модуль-5-комнатный-декор)
7. [Клавиши](#клавиши)
6. [Контент при вкладке креатива](#вкладка-креатива)
7. [Звук и атмосфера](#звук-и-атмосфера)
8. [Известные замечания](#известные-замечания)
9. [Сборка и CI](#сборка-и-ci)

---

## Установка

| Компонент | Версия |
|---|---|
| Minecraft | 1.21.1 |
| Fabric Loader | 0.16.5+ |
| Fabric API | 0.102.0+1.21.1 |
| Java | 21 |

Джарник `serverrack-1.0.0.jar` берётся из артефактов GitHub Actions
(Actions → последний зелёный ран → `serverrack-jar`), кладётся в `mods/`.

---

## Модуль 1: Серверные стойки

Три мультиблок-стойки, ставятся одним блоком и растут вверх:

| Блок | id | Высота | Свет (актив/неактив) |
|---|---|---|---|
| Серверная стойка — маленькая | `server_rack_basic` | 2 | 10 / 4 |
| Серверная стойка — большая | `server_rack_advanced` | 3 | 12 / 5 |
| Серверная стойка — мейнфрейм | `server_rack_mainframe` | 4 | 13 / 5 |

- **Клавиша J** — вкл/выкл **все стойки** в радиусе 32 блоков разом.
  Логика: если хоть одна выключена — включаются все; иначе гаснут все.
  В actionbar пишется, сколько стоек найдено (или «не найдено»).
- Активные стойки светятся, из-под потолка над ними собирается **дым**.
- **Дым** (клиентский менеджер): частицы ползут по потолку от точки генерации.
- **Меню дыма — клавиша U**: частота эмиссии, частиц за раз, радиус по потолку,
  максимальная высота подъёма, плотность. Настройки синхронизируются
  по сети (C2S/S2C payload-ы), общие для сервера.
- **Гул кулеров**: ближайшая активная стойка в радиусе 20 блоков тихо визжит
  вентиляторами (клиентский локальный звук, на сцене его не слышно — только
  у камеры; луп ≈2,3 сек, голос один на стойку).

## Модуль 2: ПК-сетап и видеостена

Один предмет `pc_wall` разворачивает структуру **3×3**:

- нижний ряд — три сегмента стола (лево/центр/право),
- верхние два ряда — **6 мониторов 16:9** (3 колонки × 2 ряда).

Ломается целиком, детирование структур — по origin (desk_center).

- **Клавиша Z** — режим «**СЕРВЕРАМ ПИЗДА**»: все ПК-сетапы в радиусе 32
  переключаются в alert. **Полная надпись горит на КАЖДОМ мониторе отдельно**
  (своя текстура 64×36 на моник, не сетка из кусочков).
  Звук подтверждения один — из центра, без хора.
- Экраны режутся из общих текстур (alert 64×36 на монитор; анимированная
  «матрица» 96×864 = 9 кадров 96×96, frametime 2), UV с компенсацией на рамки.
- **Органика**: у каждого монитора свой мелкий сдвиг по высоте (−0.4..+0.35)
  и глубине (−0.03..+0.08), зональные оттенки матрицы (янтарь/голубой/тускло),
  верхне-правый монитор шумит, нижне-правый фликерит.
- Alert светит ярко (luminance 14), в обычном состоянии — 9.

## Модуль 3: Режиссёрский детонатор

Предмет `detonator` (макс. стак 1). Полностью независим от стоек и ПК —
работает с любым миром/постройкой.

### Жесты

| Жест | Действие |
|---|---|
| **ЛКМ по блоку** | Точка 1 (блок защищён — не ломается) |
| **ПКМ по блоку** | Точка 2 → мгновенный контур + **авто-бэкап на диск** |
| **ПКМ в воздух (тап)** | Меню детонации |
| **ПКМ в воздух (удержать ~1,5 сек)** | Меню настроек ядерного гриба |
| **Shift + ПКМ в воздух** | Снять выделение |
| **Клавиша P** | БАБАХ сразу, без меню |

### Виды взрывов (11)

Шоквейв, Фитиль (змейка), Панкейк (этажи вниз), Шрапнель (хаотично всё),
Двухфазный (сперва пожар — потом детонация), **Расплавление ядра**
(искры → грибовидный взрыв), Залп (зенитки по секциям), Имплозия
(всё втягивает к центру), Срез (плоскость из края в край), Артобстрел
(снаряды сверху по столбам), **Ядерный гриб (ЖЁЛТЫЙ)**.

Гриб собирается из **7 видов отрисованных частиц**: шапка, ножка, ядро огня,
кольцевая волна, дымовые завихрения, искры и свечение — вспышка → выжигание
кратера → кольцо по земле → рост ножки → раскрытие шапки.

**Меню гриба (удержать ПКМ в воздухе)** — как у дыма, только для нюка:
высота ножки (4–40, деф. 14), радиус шапки (3–20, деф. 8), радиус кольца
(4–28, деф. 10), плотность частиц (5–100, деф. 40). Синхронизируется по сети
и подгружается при входе на сервер.

### Темп и слоумо

В меню: ×1 / ×0.5 / ×0.25. При темпе <1 сервер реально замедляется
через `TickManager` (20·speed TPS) — Replay Mod записывает это как
тяжёлую киношную сцену. Как только все работы кончились — TPS сам
возвращается к 20.

### Бэкап и восстановление

- Авто-бэкап пишется в `<мир>/serverrack_backups/<uuid игрока>_<измерение>.nbt`
  асинхронно (без лаг-спайка большого региона).
- Сохраняются: все блоки (палитра + id), block entities (сундуки и т.п.),
  закреплённые сущности — item-фреймы, стойки, картины. **Мобы и игроки
  в бэкап не попадают.**
- Бэкап лежит на диске и **переживает перезаход** — можно снимать серией.
- «Восстановить сцену» — послойная сборка снизу вверх (~2 слоя/тик),
  мерцание END_ROD по новому этажу; сущности возвращаются в конце.
- **Уборка последствий**: вместе со структурой восстановление подметает
  съёмочную площадку — убирает разлетевшиеся падающие блоки и выброшенный
  дроп вокруг сцены (+3 блока за контуром) и гасит оставшийся огонь внутри.
  После «Восстановить» локация снова чистая, как до дубля.
- «Бэкап заново» — переснимает выделение в файл (на новый дубль).

### Разрушение — реализм без бардака

- Лута **нет**, дублей **несколько** — как на съёмочной площадке.
- Инвентари уничтожаемых контейнеров чистятся до нуля.
- ~10% шанс зацепить блок **сразу за границей** выделения — грязный
  живой край, как у настоящего взрыва.
- Падающие блоки летят с импульсами от эпицентра (или к центру — в Имплозии).
- Частицы: обломки блоков, огонь, дым, искры, вспышка, грибовое облако при
  мелтдауне; звуки взрывов и гуда огня с позиционированием по сцене.
- **Бумажки**: 2.5% шанса на уничтоженный блок — офисный мусор.
  6 видов с весами: помятая/согнутая/обгоревшая страница (частые),
  чертёж (средний), стикер и рваный чек принтера (редкие).
  Листочки **не валяются болтающимся дропом** — каждый приземляется
  в **невидимую рамку** на полу (рандомный поворот 0–7), лежит ровным
  слоем и не пропадает по таймеру.

## Модуль 4: АК-сетап — загрузка GTA 6

Уголок «админа контента»: один большой монитор на стойке + геймерское кресло.

### АК-монитор (`ak_monitor`)

- Ставится одним предметом, собирается мультиблоком **2×2** (справа-снизу —
  origin), экран 16:9 на центральной стойке. Ломается целиком.
- **30-секундный цикл «загрузки GTA 6»**: розовый логотип, силуэты города
  и пальма, жёлтая полоска мееедленно ползёт слева направо
  (текстура 64×1920 = 30 кадров, frametime 20 → ровно 30 секунд на круг).
- **Клавиша X** — на весь экран красным «**ОШИБКА ЗАГРУЗКИ!**» на всех
  АК-сетапах в радиусе 32 блоков (логика как у стоек: хоть один спокоен —
  краснеют все; иначе все возвращаются к загрузке). Повторный X — обратно.
  Звук-тамблер один, из центра.

### Геймерское кресло (`ak_chair`)

- **ПКМ по креслу — сесть.** Седло — невидимая маркерная стойка для брони
  (`Marker:1b` собирается через NBT: с 1.20.5 `setMarker` приватный),
  так что зад садится ровно на подушку, а не на hitbox стойки.
- Встать — как с лодки (Shift/снеак по ванилле). Одно кресло — один зад:
  занятое кресло второго не пускает.
- Стул-уборщик: раз в 40 тиков пустые «седла» подметаются, кресла
  не остаются призрачными. Сломал кресло — седло уходит вместе с ним.

## Модуль 5: Комнатный декор

Шесть мелких предметов, чтобы офис/серверная не были голым бетоном:

| Предмет | id | Что это |
|---|---|---|
| Кружка | `decor_mug` | кружка на столе |
| Мусорное ведро | `decor_bin` | корзина для бумаг |
| Доска для записей | `decor_board` | планёрка на стену (4 ориентации) |
| Офисное растение | `decor_plant` | куст в кашпо |
| Стопка документов | `decor_papers` | бумажная горка |
| Wi-Fi роутер | `decor_router` | коробочка с антенной, LED мигает (2 кадра) |

## Клавиши

| Клавиша | Название в настройках | Что делает |
|---|---|---|
| **J** | Дым: ВСЕ стойки в радиусе | Переключает активность всех стоек в 32 блоках |
| **Z** | ПК-стены: режим СЕРВЕРАМ ПИЗДА | Alert всем ПК-сетапам в 32 блоках + звук |
| **X** | АК: ОШИБКА ЗАГРУЗКИ! | Красный экран ошибки / обратно на загрузку GTA 6 |
| **U** | Меню дыма | GUI настройки потолочного дыма |
| **P** | Детонатор: БАБАХ | Мгновенный взрыв текущего выделения |

## Вкладка креатива

Вкладка «Серверные стойки» (itemGroup `server_racks`), 19 предметов:

`server_rack_basic` · `server_rack_advanced` · `server_rack_mainframe` ·
`pc_wall` · `detonator` · `paper_scrap_a` … `paper_scrap_f` ·
`ak_monitor` · `ak_chair` · `decor_mug` · `decor_bin` · `decor_board` ·
`decor_plant` · `decor_papers` · `decor_router`

## Звук и атмосфера

- **Гул кулеров** у активных стоек — клиентский локальный (см. модуль 1).
- Тумблер ПК-алерта и АК-ошибки играет звук один раз из центра действия.
- При взрывах — взрывные/огненные звуки по сцене блоками, без спама.

## Известные замечания

- Старые модели стены `m_*3` (сетка 3×4) удалены — постройки, сделанные
  на них в ранних версиях, будут битыми. Дев-мод, перестрой сетапы.
- GUI на скринштах не светится — для съёмки с Replay Mod снимай со стороны
  (как и планировалось).
- Английская локаль — описательная, основной язык ролика — русский (`ru_ru`).

## Сборка и CI

```bash
./gradlew build   # Java 21, gradle 8.10.2 (wrapper), fabric-loom 1.8-SNAPSHOT
```

GitHub Actions собирает ветку и PR, артефакт `serverrack-jar`.
Актуальный пул-реквест: **#2** — стойки + ПК-стена + детонатор с нюком +
АК-сетап с креслом + декор, зелёные чеки, mergeable.

---

Все текстуры — попиксельные, сгенерированы кодом (Python, без внешних
редакторов); модели — детальные JSON, генераторами. Ничего «AI-картиночного».
